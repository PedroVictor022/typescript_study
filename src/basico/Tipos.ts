/* eslint-disable */
let nome: string = "Pedro"; // All strings
let idade: number = 99; 
let adult: boolean = true; // true or false
let simble: symbol = Symbol("All simbles");

// Functions
function soma(x: number, y: number): number {
  return x + y;
}

console.log(soma(10, 2))